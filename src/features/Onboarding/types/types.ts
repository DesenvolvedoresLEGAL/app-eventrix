export interface BrazilianState {
    code: string;
    name: string;
    region: string;
    capital: string;
}

export interface BusinessSegment {
    code: string;
    name: string;
    descriptions: string;
}